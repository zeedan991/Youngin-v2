/**
 * Chatbot Module (MVP Stub)
 
 */

export function initChatbot() {
    console.log('Chatbot Module: Not loaded in MVP mode.');
    const toggle = document.getElementById('chatbot-toggle');
    if (toggle) {
        toggle.style.display = 'none';
    }
}
